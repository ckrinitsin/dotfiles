return {

    {
        "numToStr/Comment.nvim",
        opts = {
            -- Visual mappings
            opleader = {
                line = '<Space>/',
                block = '<Space>?',
            },

            -- Normal mappings
            toggler = {
                line = '<Space>/',
                block = '<Space>?',
            }
        },
        lazy = false
    }

}
